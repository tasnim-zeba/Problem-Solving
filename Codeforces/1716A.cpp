#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin.exceptions(ios::badbit | ios::failbit);
    ll t,i,n,j;
    cin>>t;
    while(t--)
    {
    {
        cin>>m;
        ll a[2][m+1];
        for(i=0;i<2;i++)
        {
            for(j=0;j<m;j++)
            {
                cin>>a[i][j];
            }
        }

    }
}
